service: go-aws-lambda-hello

frameworkVersion: "3"

provider:
  name: aws
  runtime: go1.x

functions:
  hello:
    handler: bin/hello
    events:
      - http:
          path: hello
          method: get

package:
 exclude:
   - ./**
 include:
   - ./bin/**
